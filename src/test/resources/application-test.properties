# Server
server.port=8080

# Data Paths (test - use temp directory)
app.images.dir=${java.io.tmpdir}/voltherm-test/images
app.pdfs.dir=${java.io.tmpdir}/voltherm-test/pdfs
app.products.json=${java.io.tmpdir}/voltherm-test/products.json
app.inquiries.json=${java.io.tmpdir}/voltherm-test/inquiries.json
app.contact-info.json=${java.io.tmpdir}/voltherm-test/contact-info.json
app.certificates.json=${java.io.tmpdir}/voltherm-test/certificates.json

# Admin Credentials (test)
admin.username=admin
admin.password=admin123

# Logging
logging.level.root=INFO
logging.level.com.voltherm=DEBUG

# CORS Configuration
cors.allowed.origins=http://localhost:3000
